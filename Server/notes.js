// Note 

/*

1. Need to install dependencies for my server
    a. express
    b. mongoose
    c. nodemon
    d. jsonwebtoken
    e. bcryptjs ---> Used for hashing password securely
    f. cookie-parser
    g. dotenv
    h. cors

2. While creating model, if you have options then use enum 
    for ex:
        role:{
            type:String,
            required:true,
            enum:["student","recruiter"]
        }

*/